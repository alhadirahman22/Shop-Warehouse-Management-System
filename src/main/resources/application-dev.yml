spring:
  datasource:
    url: jdbc:mysql://localhost:13306/warehouse_inventory
    username: root
    password: root123t4ng
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8012
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
